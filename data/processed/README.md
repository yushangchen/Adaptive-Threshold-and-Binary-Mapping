The recommended standardized file is:

```text
ATBM_dataset.mat
```

Create it with `scripts/workflows/prepare_dataset_from_lvm.m` or `atbm.loadLVMFolder(...,'SaveTo',...)`.
Processed experimental data are excluded from Git.
